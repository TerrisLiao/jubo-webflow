# 05 待辦與待確認

**執行順序是 P0 → P1 → P2。不要先做批次下架，也不要先寫 pillar。**

原則：**先降風險（🔴 授權）→ 再清垃圾（T4 下架）→ 最後才立 pillar。** 前兩者是已存在的法律與索引成本，pillar 是機會成本；而且下架做完之後，pillar 要吸收的素材清單才會收斂。

標【本機】的只能在 Terris 的 Mac session 執行（見 `README.md` 的執行環境說明）。

---

## P0：開工阻擋

- [ ] **P0-1 查清「301 Redirect」欄位的實際行為** ⚠️ 全案最高風險
  - [ ] 查新聞模板頁與 Slater `60292.js` 有沒有讀 `301-redirect-2` 欄位（起點 `custom-code/slater-selectors.md`）
  - [ ] 抓一篇已填該欄位的**線上 HTML 實測跳轉**，不能只信 API
  - [ ] 確認站台層級 301 的位置（Webflow Site settings → Publishing → 301 Redirects）與方案剩餘條數（**需要 30–40 條**）
  - [ ] 條數不夠 → 改採「保留頁面 + 移出 sitemap」備案，並在 `03_` 記錄原因
  - 為什麼阻擋：CMS item 欄位若只是 client-side 轉址，item 一 unpublish 轉址就消失、URL 直接 404，「Unpublish + 301」整個策略失效

- [ ] **P0-2 釐清「照管平台」的 URL 與搜尋意圖**
  - Notion 記載 `照管平台` 曝光 **5,681** 但 CTR 僅 **0.04%**，並註明「先確認其對應 URL 與搜尋意圖，暫不列為內容目標」
  - 線上已有 `ltc-system-guide`（照管平台是什麼？）與 `ltc-system-guide02`（核銷）
  - [ ] 【本機】用 CrawlSEO striking-distance 報表確認這 5,681 次曝光落在哪個 URL
  - **若落在無關頁面，那是 cannibalization 問題而非內容缺口，處理方式完全不同**

- [ ] **P0-3 確認 3 篇卡稿 draft 的現況**
  - `11_` 記載卡在 draft：`amy-lense-intro-2026`（卡 9 天，而 `/ai/amy` 已上線且有完整 JSON-LD）、`article-9529`、`article-9507`（`short-summary` 也空白）
  - 這 3 篇 2026-09-03 都在 sitemap 清單且 `includeInSitemap: true`，看起來已發布
  - [ ] **逐篇讀回 `isDraft` 確認**，不沿用三個月前的稽核結論

- [ ] **P0-4 決定 `14_Webflow_Agent Instructions同步規範.md` 怎麼處理**
  - `marketing-ops-stack/AGENTS.md` 要求 Webflow 變更前讀 jubo-webflow 的這份文件，**但它在 jubo-webflow 不存在**（目前編號到 `12_`，且缺 `10_`）
  - [ ] Terris 決定：補寫這份規範，或修正 `AGENTS.md` 的引用
  - 本專案只記錄落差，**不順手新增規範文件**

- [ ] **P0-5 【本機】CrawlSEO 資料準備**
  - [ ] `docker compose ps` 確認兩個服務 healthy
  - [ ] 跑一次 GSC sync（property **`https://www.jubo-health.com/`**；`sc-domain:jubo-health.com` 目前 403，**不可當主要 property**）
  - [ ] 取得四張報表：pages / keywords、**content decay**、**cannibalization**、**striking-distance**
  - [ ] sanitized 結果放進 `data/`（已 .gitignore）
  - **不改 compose、不改 schedule、不讀 `.env`**；`run_crawl` 是寫入動作，要人工授權

---

## P1：盤點與風險清理

- [ ] **P1-1 87 篇批次盤點**
  - [ ] 用 `list_items_sitemap_status` 列出全部 87 筆（輕量，不含 Rich Text）
  - [ ] 切 18 批，每批 4–6 篇，subagent 各寫 `batches/批次NN.md`
  - [ ] 合併進 `02_逐篇盤點表.md`，總列數必須是 87，`slug` 無重複無遺漏
  - 方法與欄位定義見 `02_`

- [ ] **P1-2 join 排序資料並產出分流建議**
  - [ ] 把 `data/` 的 GSC 資料 join 進盤點表
  - [ ] 填授權燈號、主題群組、T1–T5 建議
  - [ ] 拿不到資料的篇目**標示「驗證限制：無 GSC 資料」，不得直接判定下架**

- [ ] **P1-3 Terris 逐列確認 87 筆分流** ← 最重的一次人力投入，可能佔滿一個半天

- [ ] **P1-4 🔴 授權處理**
  - [ ] 整理成一頁決策表（`04_`），附原文出處推測與建議做法
  - [ ] Terris 逐篇選 (a) 找授權 / (b) 改摘要 / (c) 下架
  - [ ] 天下雜誌的合作條款單獨確認（`04_` 有專節）
  - [ ] 執行 (b) 的改摘要版本，寫成 Draft → Terris 審 → **人工 Publish**

- [ ] **P1-5 T4 下架**
  - [ ] 完成 `03_下架與301對照表.md`
  - [ ] 分兩批下站台層級 301 + unpublish
  - [ ] **逐條 `curl -sIL` 實測**跳轉，結果與日期填回對照表（不是「已設定」而是「已驗證」）

- [ ] **P1-6 T5 兩篇 landing page 歸位**
  - [ ] `vitallink-landing`（`6a55d2cb423c1ab08f2d1cac`）、`daycare-banner-landing-page-1`（`6a57372d0146da925de6257d`）移出 sitemap
  - [ ] 文件記錄「這是 landing page 不是文章，`/news` 統計要排除」
  - **不動 URL** —— 可能有廣告在導流。搬家另案，需先確認投放狀態

---

## P2：pillar 與機制

- [ ] **P2-1 四支 pillar**，依 GSC 訊號強弱排序，一次一支
  - [ ] 護理之家照護管理系統（曝光 360／排名 7.78）
  - [ ] 照護管理系統（121／4.98）
  - [ ] 長照系統（核心支柱頁）
  - [ ] 居家照護管理系統（59／6.69）
  - 規格見 `06_`。**一支沒過就不推下一支，不趕進度**
  - 每支：pillar 是**新建 CMS item（新 slug）**，舊文以站台層級 301 導入。**不要把舊文改寫成 pillar 再改 slug**

- [ ] **P2-2 T3 輕修**（約 30 篇，與 pillar 並行分批）
  - [ ] 補 `short-summary`
  - [ ] 補圖片 alt（**資訊型**：含活動時間／講者／圖表數字，不是「示意圖」）
  - [ ] 分類歸位
  - **不改寫成問句標題** —— 公告本來就不是知識型內容

- [ ] **P2-3 T1 補目錄**（H2 > 3 的合規文章）

- [ ] **P2-4 建立機制**
  - [ ] 新鮮度 SLA 表：法規／評鑑／資安／長照 3.0，各指定**內容所有人**與更新週期（`07_`）
  - [ ] 30 天回檢流程（`07_`）
  - [ ] sitemap 與 `includeInSitemap` 一次校正
  - [ ] 回頭校正 Notion 文章清單（表上 #5 / #9 / #11 未打勾，但線上已有 `ltc-setup-checklist` / `infosec` / `daycare01`）
  - [ ] `11_AEO稽核與優化計畫.md` 的 #19（加目錄）、#22（alt）、#23（清 draft 與節奏）標注「已由本專案承接」。**不改它的稽核結論** —— 那是 2026-08-20 的歷史快照

---

## 明確不做

- 不改新聞模板結構（加 `<main>`/`<article>`、把 25 個相關文章 h2 降級）→ `11_` #11／#12，模板層級，另排
- 不處理站台 JSON-LD 事實錯誤（假評分、`numberOfEmployees: "330000"`）→ `11_` P0-A，另排
- 不做《智齡數據報告》落地頁復活 → Notion P0、權威缺口最大，但屬新建落地頁，**另開專案**
- 不動共用 Component / utility class / Variable / tag selector，不新增自訂 CSS
- 不碰 `/jp`
- **不刪任何 CMS item**
- 不改 CrawlSEO 部署設定
