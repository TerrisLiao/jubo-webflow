# 17_效能稽核：首頁載入速度診斷

- 建立日期：2026-09-03
- 對象：`https://www.jubo-health.com/`（首頁）
- 起因：回報官網載入明顯偏慢，且無痕視窗同樣慢（排除本地快取／擴充套件因素）
- 更新：2026-09-03 第二輪複驗，修正兩處過度推論、補上歷史比對與 Swiper 全站掃描結果

## 驗證限制（先讀）

本次診斷以 HTTP 層實測完成，**不是**真實瀏覽器 Lighthouse 跑分：

- 量測機房位於 **AWS us-east-1（維吉尼亞）**，不是台灣。以下所有秒數皆為**最佳情況**；台灣使用者對非 CDN 主機的延遲約為此處的 2–3 倍。
- 本環境的 Chromium 無法穿過 egress proxy（`ERR_CONNECTION_RESET`），因此**沒有實測的 FCP / LCP / TBT / CLS 數字**。
- 相關結論凡屬推估者，下文均標記為「推估」。
- 建議由台灣端補跑 PageSpeed Insights / WebPageTest（Taipei node）取得真實 Core Web Vitals，再據以複驗。

### 第一版的兩處更正

第一版報告有兩點講過頭，這裡更正：

1. **slater.app 的延遲數字被高估。** 首次量到的 `tls: 0.40s / ttfb: 0.45s` 是 proxy 冷啟動的假象。連續 5 次穩態量測後，實際是 slater `ttfb ≈ 0.18s`、jsdelivr `≈ 0.13s`、Webflow CDN `≈ 0.14s`。**從美東看，slater 只慢約 50ms，不是「特別慢」。** 真正的差距在台灣端才會放大（見下）。
2. **Webflow 已對 slater CSS 發送 Early Hints preload。** 實測 `HTTP/2 103` 回應中確實含有 `<https://slater.app/20018/60294.css>; as=style; rel=preload`。也就是說這支檔案的連線與下載**在 HTML 主體到達前就已經開始**，第一版說的「瀏覽器要等 HTML 解析到才發出請求」並不成立。因此「補 preconnect」這條建議作廢。

## 這個問題之前就存在嗎？

**結論：是。這不是最近才出現的迴歸，而是 Webflow 版官網上線起就帶著的結構。**

證據與其限制：

| 查證項目 | 結果 |
|---|---|
| Wayback Machine 存檔 | **沒有任何 Webflow 版的存檔。** 最後一筆 `www.jubo-health.com` 存檔是 2026-06-06，內容仍是舊站 |
| 舊站平台 | `<meta name="generator" content="WordPress 7.0">` + WooCommerce 10.8.1；2025-10 那筆則是 WordPress 6.8.3 + Elementor 3.21.4 |
| 舊站是否有 slater | **無。** 2025-10 / 2026-01 / 2026-05 / 2026-06 四筆存檔中，`slater.app`、`gsap`、`lenis`、`webfont.js` 出現次數皆為 0 |
| Webflow 站建立時間 | Site ID `69ec2b02` 解為 **2026-04-25**；素材 ID 最新為 `6a7549a3` = 2026-08-07 |
| repo 首次記錄 Slater | 2026-08-18（`custom-code/slater-selectors.md`），2026-08-19 commit `9d9f055` |
| `no-store` 是否為近期變動 | 兩支檔案（`60292.js`、`60294.css`）目前皆回 `no-store` + `pragma: no-cache`，`x-runtime` 僅 5–7ms。這是 Slater 平台的一貫行為，不是站方設定 |

因此可以確定：**Slater 的 `no-store` 從 Webflow 版上線第一天就存在。** 若感覺「最近特別慢」，成因比較可能是內容量增長，而不是設定被改壞。

### 舊站 vs 新站的量體比較

| 項目 | 舊站（WordPress，2026-06-06 存檔） | 新站（Webflow，2026-09-03） |
|---|---|---|
| HTML 大小 | 174.9 KB | **534.4 KB（3.1 倍）** |
| 元素數 | 約 898 | 約 2,040 |
| `<img>` 數 | 19（全部 lazy） | 51（37 lazy / 14 eager） |
| 外部 script | 43 支（29 支無 defer/async） | 15 支（13 支無 defer/async） |
| stylesheet | 39 個 | 3 個 |
| 第三方 host | 9 | 12 |

要講公道話：**舊 WordPress 站的 script 與 stylesheet 數量其實更誇張**（43 支 script、39 個 CSS），它也不快，只是慢法不同——當年多數資源同源、可快取。新站把檔案數大幅收斂了，但換來三個新問題：HTML 膨脹成 3 倍、單頁 JS 約 500 KB、以及一個不可快取的第三方關鍵路徑資源。

**限制：** 因為 Wayback 沒有 Webflow 版存檔，我無法量測「新站剛上線時」與「現在」的差異。如果你的體感是最近幾週才變慢，最可能的變因是 8 月新增的內容與素材（例如 2026-08-07 上傳的 353 KB webp），這點需要 Webflow 的發布紀錄才能確認。

---

## 結論摘要

HTML 本身不慢（Cloudflare 邊緣快取命中，TTFB 約 0.20s）。**慢在子資源，主因集中在 `slater.app`。**

| # | 問題 | 嚴重度 | 可否處理 |
|---|---|---|---|
| **0** | **`.home-gradient-bg` 滿版動畫 `blur(50px)`，且元素重複兩份** | **極高** | **可，改一行 CSS** |
| 1 | Swiper 約 170 KB 全站載入，161 頁沒有一頁在用 | 高 | 可，且無風險 |
| 2 | `<head>` 有 3 支 render-blocking script（webfont.js 最嚴重） | 高 | 可 |
| 3 | body 有 10 支 blocking script，未壓縮約 500 KB | 中—高 | 可（加 defer） |
| 4 | 圖片未優化，最大單張 841 KB PNG | 中 | 可 |
| 5 | HTML 534 KB，含 79 個 inline SVG | 中 | 可，但需 Designer 工時 |
| 6 | 主 CSS 322 KB（brotli 55 KB） | 中 | 可，但需清 class |
| 7 | 裸網域 301 跳轉 `cf-cache-status: BYPASS` | 低 | 需 Cloudflare 權限 |
| 8 | slater.app `no-store` + 單一美東來源 | 中 | 不可 |

**第 0 項與第 1–8 項不是同一個量級。** 前者決定頁面順不順（幀率），後者決定頁面多久載完（秒數）。使用者體感的「lag」主要來自第 0 項。

### 實測載入數字（CPU 4x 節流 + 200ms RTT / 20Mbps）

| 指標 | 數值 | 評價 |
|---|---|---|
| FCP | 1248ms | 尚可 |
| LCP | 1248ms（`H1.text-align-center`） | 良好 |
| CLS | 0.0000 | 良好 |
| **TBT** | **1642ms** | **差**（Google 門檻：<200ms 良好、>600ms 差） |
| load | 3999ms | 偏慢 |
| 請求數 / 傳輸量 | 76 / 0.48 MB | 尚可 |

主執行緒工作分類：Scripting 2265ms、Parse 862ms、Style 713ms、Layout 666ms、Paint 217ms。
CPU 歸屬前三名：`webflow.schunk.5cb2512d` 375ms、**GTM + gtag 446ms**、GSAP + ScrollTrigger 299ms。
另發現 `@finsweet/attributes@2` 會再展開約 22 個 chunk 請求，形成 3–4 層的序列瀑布。

---

## 0. 真兇：`.home-gradient-bg` 的全螢幕動畫模糊（2026-09-03 第三輪，實機瀏覽器實測）

> 前兩版報告只量了網路層，結論是「880 KB 可以省」。使用者質疑這解釋不了體感，**質疑是對的**。
> 這一輪把 Chromium 跑起來實測後，找到一個影響遠大於前面所有項目加總的東西。

### 症狀

同一台機器、同一套測法（40 次滾輪 × 180px），比較捲動幀率：

| 網站 | 中位幀距 | 幀率 | 掉幀率 |
|---|---|---|---|
| **Jubo 首頁** | **133.3ms** | **8 fps** | **98%** |
| Webflow 官網（同為 Webflow 站） | 16.7ms | 60 fps | 13% |
| GSAP 官網（重動畫站） | 16.7ms | 60 fps | 1% |
| Wikipedia（極輕長頁） | 16.7ms | 60 fps | 0% |

**關掉 CPU 節流也一樣是 8 fps**，代表瓶頸不是 JS 算不完，而是繪製本身。

### 逐層剝除實驗

| 條件 | 幀率 |
|---|---|
| 原樣 | 8 fps |
| 只關 `backdrop-filter`（117 個元素） | 8 fps（**無效**） |
| 只關 `.gradient-bg` 的 blur | 8 fps（**無效**） |
| **只移除 `.home-gradient-bg` 一個元素** | **60 fps** |

**一個元素，8 fps → 60 fps。**

### 成因

`.home-gradient-bg` 是一個 `position: fixed; inset: 0; z-index: -1` 的滿版元素（1440×900），身上疊了：

1. **5 層 `radial-gradient`**，圓心位置綁在 `--x1`…`--y5` 十個 CSS 自訂屬性上
2. 這些屬性用 `@property { syntax: '<percentage>' }` 註冊，**因此可被逐格內插**
3. 三條 `infinite alternate` 動畫持續改變它們：

```css
animation: move1 5s ease-in-out infinite alternate,
           move2 6s ease-in-out infinite alternate,
           move3 7s ease-in-out infinite alternate;
```

4. 最上面再蓋一層 **`filter: blur(50px)`**

於是每一格畫面，瀏覽器都必須：重算 10 個自訂屬性 → 重新產生 5 層滿版漸層 → 對 1440×900 整片做 50px 半徑模糊。而且 `infinite` 代表**這件事永遠不會停，使用者完全不操作也在燒**。

### 而且它有兩份

實測 `document.querySelectorAll('.home-gradient-bg').length === 2`：

| # | 來源 | 父層 |
|---|---|---|
| #1 | Webflow 頁面內的 Embed 元件 | `DIV` |
| #2 | **slater.js 第 789–791 行動態插入** | `BODY` |

```js
const bg = document.createElement('div');
bg.className = 'home-gradient-bg';
document.body.appendChild(bg);
```

Slater 又補了一個 Webflow 裡已經存在的元素，**成本因此加倍**。這是 bug，不是設計。

### 不只首頁

| 頁面 | `.home-gradient-bg` 數量 | filter |
|---|---|---|
| `/`（首頁） | 2 | `blur(50px)` |
| `/news` | 2 | `blur(50px)` |
| `/contact` | 2 | `blur(50px)` |
| `/jubo-ai` | 1 | `none` |

### 關鍵：這個 blur 幾乎沒有視覺價值

比對移除 blur 前後的截圖，**外觀幾乎無法分辨**。因為底下本來就是 `transparent 100%` 收尾的柔和 radial-gradient，再模糊 50px 加不到什麼東西。

**付出整頁幀率，換到看不出來的差別。**

### 建議修法（皆不需要碰 Slater 的 JS）

1. **拿掉 `filter: blur(50px)`。** 這行在頁面內的 Embed `<style>` 裡，Designer 可直接改。若覺得漸層邊緣不夠柔，把 radial-gradient 的擴散範圍調大即可，成本是零。
2. **處理重複元素。** 兩個做法：Webflow 頁面裡的那份保留、請 Slater 端移除第 789–791 行；或反過來。**這需要與 Slater 維護者確認，是本項唯一需要外部協調的部分。** 在那之前，光做第 1 點就能拿回幀率。
3. 若要保留動態感：改用 `transform: translate()` 移動幾個已模糊的靜態圖層，`transform` 走合成器、不需重繪。

### 驗證限制（重要）

- 本次測試的 Chromium 使用 **SwiftShader 軟體繪圖**（`ANGLE (Google, Vulkan 1.3.0 (SwiftShader Device))`），**沒有真實 GPU**。因此「8 fps」這個絕對數字**不等於你機器上的數字**——有 GPU 加速時模糊會便宜得多。
- 但**對照組是決定性的**：同一個軟體繪圖環境下，Webflow 官網、GSAP 官網、Wikipedia 全都跑滿 60 fps，只有 Jubo 是 8 fps。這代表 Jubo 的繪製成本在同儕中是異常高的，不是環境問題。
- 實務上，滿版持續動畫的 `blur(50px)` 在整合顯示晶片的筆電與手機上一向是已知的重度效果。實際影響程度仍需台灣端在真實機器上驗證。

---

## 1. Swiper 全站零使用（次高優先，且無風險）

全站每一頁都載入：

- `swiper-bundle.min.js` — **155.3 KB**
- `swiper-bundle.min.css` — 14.6 KB

合計約 **170 KB**。

實測掃描結果：

| 掃描對象 | `swiper-wrapper` / `swiper-slide` / `new Swiper` / `class="swiper` |
|---|---|
| sitemap.xml 全部 **161 個頁面** | **0** |
| `slater.app/20018/60292.js`（92.9 KB） | **0**（不分大小寫） |
| `slater.app/20018/60294.css` | **0** |

**全站沒有任何一處在使用 Swiper。這 170 KB 是純粹的死重。**

### 順帶更正一份既有文件

`custom-code/slater-selectors.md` 記載 60292.js 的內容是「GSAP／**Swiper** 驅動的互動」。實測該檔案中 `swiper` 出現 **0 次**，此描述有誤，應更正為「GSAP 驅動」。首頁的 cases slider 是 GSAP 實作（檔案內有 `//Home Cases Slider` 註解區塊）。

### 建議

從 Site Settings → Custom Code 移除這兩行。這不涉及 Slater、不涉及任何 class、不影響任何互動。

> 移除前仍請照 CLAUDE.md 規範，於 Designer 內確認沒有 Embed 元件另行引用 Swiper，並先在 staging 驗證。

---

## 2. `<head>` 有 3 支 render-blocking script

| 腳本 | 屬性 | 大小 |
|---|---|---|
| `ajax.googleapis.com/.../webfont.js` | **無 defer/async** | 13.2 KB |
| `cdn.jsdelivr.net/npm/lenis@1.1.13/...` | **無 defer/async** | 13.5 KB |
| `.../@flowbase-co/boosters-countup@1.0.0/...` | **無 defer/async** | 2.2 KB |

這三支會直接卡住 HTML parser。其中 `webfont.js` 問題最大：它只是為了載 Poppins 5 個字重，卻造成三段**序列**往返：

```
webfont.js (ajax.googleapis.com)
  → fonts.googleapis.com/css   （JS 執行後才發出）
    → fonts.gstatic.com/*.woff2
```

文字要到第三段回來才會用正確字體渲染。

### 建議
- 移除 `webfont.js`，改用 Webflow 內建的 Google Fonts 設定，或直接在 `<head>` 放 `<link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Poppins:wght@300..700&display=swap">`。`display=swap` 可避免文字被隱藏。
- `lenis` 與 `countup` 加上 `defer`。兩者都不影響首屏內容。
- 補充：CSS 中的 `Noto Sans TC` 沒有對應 `@font-face`，實際走系統字（PingFang TC / 微軟正黑體）。這點是好的，維持現狀即可，不要改成從 Google Fonts 載中文字體（中文字體檔非常大）。

---

## 3. `<body>` 有 10 支 blocking script

依 DOM 順序，全部無 `defer`／`async`：

| 腳本 | 未壓縮大小 |
|---|---|
| jquery-3.5.1.min.js | 89.5 KB |
| webflow.schunk × 2 + webflow.*.js | — |
| gsap.min.js | 72.9 KB |
| ScrollTrigger.min.js | 44.6 KB |
| Draggable.min.js | 35.8 KB |
| InertiaPlugin.min.js | 7.3 KB |
| swiper-bundle.min.js | **155.3 KB** ← 見第 1 節，可直接移除 |
| slater.app/60292.js | 92.9 KB |

合計約 500 KB 未壓縮 JS，全部要下載 + parse + execute 才能互動。

### 建議
- 逐支確認首頁是否真的需要。`Draggable` + `InertiaPlugin`（43 KB）通常只有拖曳型輪播才需要。
- 能加 `defer` 的都加上（注意 slater.js 依賴 gsap，順序需一併調整）。

---

## 4. 圖片

- 不重複來源圖 54 張，最大變體加總 **3.06 MB**（含所有 srcset 變體則為 4.59 MB，瀏覽器實際只取其一）。
- 51 個 `<img>` 中 37 張 `lazy`、14 張 `eager` — lazy loading 設定是好的。

問題最大的幾張：

| 大小 | 檔案 |
|---|---|
| **841.5 KB** | `69f844f17a076a7f0ee47ce5_cases-3.png` |
| 353.4 KB | `6a7549a3ec269e2d87ad09a6_未命名設計.webp` |
| 342.1 KB | `6a112274eb37217428c40139_三三分享會-官網宣傳圖-1200x800.png` |
| 216.9 KB | `6a11227e6d8984fcaa9e00f7_DSC_0595-scaled.jpeg` |
| 172.1 KB | `6a11223f86f678134db1c98f_簽約儀式照片_1-scaled.jpeg` |
| 159.4 KB | `6a11226c5738e8a9c5856e4e_fb_智齡-e1768370217399.jpeg` |

觀察：檔名帶 double-encoding（`%25E4%25B8...`）與 `-scaled` 後綴的，是從 WordPress 搬遷過來、未重新優化的素材，集中在 CMS（`/news` 類）圖片。

### 建議
- `cases-3.png` 841 KB 轉 WebP，預期可降到 100–150 KB。這是投報率最高的單項。
- CMS 圖片建立上傳規範：一律 WebP、長邊 ≤ 1600px、單檔 ≤ 200 KB。
- 首屏 hero（`hero-img.webp`, 68 KB）大小合理，但可考慮加 `<link rel="preload" as="image">` 改善 LCP。

---

## 5. HTML 534 KB

- 原始 534,415 bytes，gzip 後 181,673 bytes。
- 元素數約 2,040（含 908 個 `div`）。
- **79 個 inline SVG，含 208 個 `<path>`、84 個 `<stop>` 漸層節點。**
- 20 段 inline `<style>`，合計 25.5 KB。
- CMS：3 個 `w-dyn-list`、30 個 `w-dyn-item`（數量合理，不是問題）。

inline SVG 無法被瀏覽器快取，每次換頁都重新傳輸一次。漸層定義特別佔空間。

### 建議
把重複使用的 SVG（logo、圖示）改為 Webflow Asset 的 `<img src="*.svg">`，即可享有 CDN 快取。這需要在 Designer 逐一調整，工時較高，建議排在 script 與圖片修正之後。

---

## 6. 主 CSS 322 KB

`jubo-health.webflow.shared.328eedf86.min.css` = 322,853 bytes（brotli 後 55 KB）。

快取設定良好（`immutable`，`cf-cache-status: HIT`），首次訪問仍是 render-blocking 的 55 KB。以 Webflow 站而言偏大，通常代表 Client-First 未使用的 class 累積。

### 建議
中期處理。搭配 `06_自訂Class完整清單.md` 與 `07_合規稽核報告.md` 做一次未使用 class 清理。**依 CLAUDE.md，刪除任何 class 前必須先查全站使用、Slater、IX2 與 Component。**

---

## 7. 裸網域 301 跳轉不被快取

```
GET https://jubo-health.com/
→ 301 https://www.jubo-health.com/
   cf-cache-status: BYPASS
   cache-control: private
```

使用者若輸入不帶 www 的網址（或舊連結、名片、QR code），會多付一次完整的 DNS + TCP + TLS + 回源往返（美東穩態實測 TTFB 約 0.20s，台灣端推估 0.5s+），且**每次都付**，因為 Cloudflare 對此跳轉是 BYPASS。

### 建議
在 Cloudflare 設定 Redirect Rule（或 Page Rule）處理 apex → www，讓跳轉在邊緣完成並可快取。這需要 Cloudflare 後台權限。

---

## 8. slater.app：問題屬實，但不可搬動

> **前提更正：** 依 `custom-code/slater-selectors.md`（2026-08-18），Slater 屬於「整個網站的背景設施，不在一般改版的討論與修改範圍內」。第一版報告把「搬進 Webflow」列為第一優先建議，**與此規範牴觸，已撤回。**

首頁載入兩個 slater 資源，皆掛在 Webflow Site Settings → Custom Code：

| 資源 | 位置 | 阻塞 | 大小 |
|---|---|---|---|
| `slater.app/20018/60294.css` | head | render-blocking CSS | 17.9 KB（gzip 3.6 KB） |
| `slater.app/20018/60292.js` | footer | blocking script | 92.9 KB（gzip 19.3 KB） |

### 確認屬實的部分

```
cache-control: no-store
pragma: no-cache
x-runtime: 0.005265
```

`no-store` 代表瀏覽器完全不得快取，每次瀏覽、每次換頁都要重抓。這解釋了「無痕視窗一樣慢」——一般視窗本來也沒有快取可用。

`slater.app` 解析到單一 A record `44.202.1.154`（AWS us-east-1），前端 `via: 1.1 Caddy`，**非 anycast、無亞洲節點**。

### 被高估的部分

- 美東穩態量測：slater `ttfb ≈ 0.18s` vs jsdelivr `≈ 0.13s` vs Webflow CDN `≈ 0.14s`。差距僅約 50ms。
- Webflow 的 `HTTP/2 103` Early Hints **已經對這支 CSS 發 preload**，連線與下載在 HTML 主體到達前就啟動了。

所以在美東，slater 的成本很小。**真正的差距在台灣端**：jsdelivr 與 Webflow CDN 在台灣有節點（RTT 約 10–30ms），slater 必須連到維吉尼亞（RTT 推估 200–250ms）。加上 TCP + TLS + request，單趟推估 0.6–0.8s，且每次載入都要付。

> **這是推估，不是實測。** 需要台灣端 WebPageTest 才能確認。

### 在不搬動 Slater 的前提下能做什麼

老實說：**不多。**

- Early Hints preload 已經在做了，補 `preconnect` 沒有額外效益。
- 把 CSS 改成非阻塞載入（`media="print"` + `onload`）技術上可行，但這支 CSS 含 `@font-face JuboFont` 與 hover／互動樣式，改非阻塞會造成 FOUC 與版面跳動（CLS）。**不建議。**
- `60292.js` 在 footer，加 `defer` 是安全的（見第 3 節），但它依賴 GSAP，必須連同 GSAP 一起 defer 以保留執行順序。

**結論：把 slater 的成本視為固定開銷接受下來，火力集中在其餘 7 項。** 光是第 1 項的 Swiper 就能省下比 slater 兩支檔案加總（110 KB）還多的量。

若未來要處理，唯一的根本解是換到可快取的託管端點；那屬於基礎設施決策，需要另案評估 Slater 方案能力與同步流程成本，不在本次範圍。

---

## 建議執行順序

**第零波（先做這個，其他都等一下）**

| 動作 | 效果 | 風險 |
|---|---|---|
| 移除 `.home-gradient-bg` 的 `filter: blur(50px)` | 捲動 8 fps → 60 fps（本環境實測） | 極低，外觀幾乎無差異 |
| 釐清並移除重複的第二個 `.home-gradient-bg` | 再省一半該元素成本 | 需與 Slater 維護者確認 |

**第一波（載入時間）**

| 動作 | 預估省下 |
|---|---|
| 移除全站 Swiper JS + CSS | 約 170 KB |
| 移除 `webfont.js`，改用 `fonts.googleapis.com` + `display=swap` | 13 KB + 消除兩段序列往返 |
| `lenis` / `countup` 加 `defer` | 解除 head 三個阻塞點 |
| `cases-3.png` 841 KB 轉 WebP | 約 700 KB |

**第二波**

5. 檢視 GTM 載入時機（目前佔 446ms CPU，是 JS 第二大宗）
6. 評估 `@finsweet/attributes@2` 是否值得那 22 個 chunk 請求
7. GSAP `Draggable` / `InertiaPlugin` 是否用到（省 43 KB）
8. 其餘 script 加 `defer`；其餘大圖優化；Cloudflare apex 跳轉

**第三波**

9. 未使用 class 清理（**刪 class 前必須先查全站使用、Slater、IX2 與 Component**）
10. 79 個 inline SVG 改為 Asset 引用

## 待確認

- **在有 GPU 的真實機器上複驗第 0 項。** 本次為 SwiftShader 軟體繪圖，絕對幀率不可外推。
- `.home-gradient-bg` 重複插入：Webflow Embed 與 slater.js 第 789–791 行該保留哪一邊？需與 Slater 維護者確認。
- **補跑台灣端 PageSpeed Insights / WebPageTest（Taipei node）**，取得真實 LCP / FCP / TBT。本報告所有「台灣端會更慢」的說法都是推估，未經實測。
- Swiper 是什麼時候、為了什麼加進 Site Settings 的？移除前確認沒有未上線的規劃用途。
- Webflow 的發布紀錄：8 月有哪幾次發布？用來驗證「最近變慢」是否對應到特定一次內容更新。
- Cloudflare 後台的操作權限歸屬。
- `custom-code/slater-selectors.md` 的「Swiper 驅動」描述需更正（本報告第 1 節）。
