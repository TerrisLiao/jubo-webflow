# 18｜`/security` 資安頁 — 設計系統對齊重建紀錄

- 日期：2026-09-17
- 站台：`jubo-health.com`（site id `69ec2b02daa2e79f1da8772a`）
- 頁面：`資安與隱私` `/security`（page id `6aaa6709b9f4c19aae5a5d4f`）
- 狀態：**維持 Draft、未 Publish**
- 背景：這頁先前由另一個 agent（Codex）建置，Terris 回報「整體頁面設計風格跟其他頁面都不一樣」，交由本次重建。

---

## 1. 問題診斷（實際讀回，非推測）

前一版的內容結構（Hero → 多層防護 → 隱私 → AI → 資源 → CTA）方向沒有錯，
**壞在執行層：它沒有用站上的設計系統，而是在旁邊另外長了一套。**

| 項目 | 實測 | 違反 |
|---|---|---|
| 頁面專屬 class | 一頁新建 **95 個** `security-*` class | 鐵律 1「先找、再用、最後才建」 |
| 手打色碼 | `#173044`、`#7f6cbc`、`rgba(64,68,112,.1)`、`rgba(71,58,119,.12)` … | 鐵律 4「顏色一律用 Variables」 |
| 自畫裝飾 | `security-network_ring-one/two/three`、`security-bento_orbit`、`security-hero_glow-left/right`、`security-bento_cloud-shape` | 15 §3「禁止假產品 UI、抽象 AI 圖」 |
| 孤兒 class | `section_security-bento`、`-bento-v2`、`security-check_*` 等前幾版殘留仍在 Style Manager | 00 §5 收工檢查表 |
| 新漸層 | Terris 要求「新創一個 gradient」，實際做法是把 `linear-gradient(135deg,#f1edff,#eaf7ff,#fff2e8)` 直接寫死在 `section_security-hero` 上，**沒有進 Variables** | 鐵律 4 |
| 文案 | `security-bento_lead` 寫著「參照日本頁的 Bento 邏輯…」——**內部工作註記外洩到正式文案** | — |
| 文案 | 「資安事件通報窗口／公開管道確認後提供」——placeholder 留在頁面上 | — |
| 裝飾字 | 卡片上放 `ID`、`↔`、`24/7`、`03` 等無意義符號 | — |

> 核心判斷：**這不是「再調一下樣式」的問題，是整頁的視覺語言來源錯了。**
> 站上其他繁中正式頁（`/products/residential-care`、`/company`）的區塊長相是
> `section-header_wrapper` ＋ `Section Tag` ＋ `3col-grid` ＋ 純文字排版，
> 不是 bento 玻璃卡。元件清單也寫明「不要讓整頁每一段都變成卡片或玻璃」。

---

## 2. 強調色決策

Terris 已指示：**不可用 `primary/Ocean`（`#175e5e`）當強調色，Ocean 平常只用在 Footer。**

本次提出三個選項，Terris 選擇 **「沿用既有漸層家族，不新增變數」**。

依據：站上本來就有一套 teal → blue → purple 的漸層語言，**不需要另外發明**。

| 既有資產 | 值 |
|---|---|
| `.gradient-headline` | `linear-gradient(90deg, var(--primary--accent), var(--gradient--announce-bar-purple))` ＋ `background-clip:text` |
| `.cases-gradient` | `linear-gradient(0deg, hsla(231,100%,61.57%,.2), hsla(184,100%,37.65%,.1) 50%, transparent)` |
| `.marquee__advanced__p.is-gradient` | `linear-gradient(90deg, hsla(184,100%,37.65%,1), #b15dff)` |

→ 本頁強調一律走 `.gradient-headline`（H1 內行內 span）＋ `Page Gradient BG` 元件背景。
**Jubo Variables 維持 33 個，本次沒有新增、修改或刪除任何變數。**

---

## 3. 重建後的結構

7 個 section ＋ 既有 `CTA Section #1`，全部沿用標準骨架
`section_* > .padding-global.padding-section-large > .container-large`：

| # | Section class | Section Tag | H2 | 版面 |
|---|---|---|---|---|
| 1 | `section_security-hero` | 資安與隱私 | （H1）守護每一筆照護資料，**從制度到技術層層把關** | `section-header_wrapper` ＋ `buttom-wrapper`（CTA Button ＋ Glass Button） |
| 2 | `section_security-layers` | 多層防護 | 從裝置到雲端，安全落在每一個接觸點 | `3col-grid` × 3 |
| 3 | `section_security-operations` | 日常維運 | 安全不是一次性認證，而是每天的維運 | `3col-grid` × 3 |
| 4 | `section_security-trust` | 國際標準 | 以可驗證的標準建立長期信任 | `4col-grid` × 4（ISO 27001／27701／13485／HIPAA） |
| 5 | `section_security-privacy` | 隱私治理 | 讓資料的蒐集、使用與保存都有清楚依據 | `3col-grid` × 3 |
| 6 | `section_security-ai` | 負責任的 AI | AI 協助照護決策，資料邊界仍然必須清楚 | `3col-grid` × 3 |
| 7 | `section_security-resources` | 進一步了解 | 需要更詳細的資安資料？ | `section-header_wrapper` ＋ Glass Button |
| — | `CTA Section #1`（元件，沿用） | — | 讓安全成為數位照護的起點 | — |

H1 內的「從制度到技術層層把關」是 `<span class="gradient-headline">`。

### 用到的 class（全部是站上既有的）

- 結構：`page-wrapper`、`main-wrapper`、`padding-global`、`padding-section-large`、`container-large`
- 版面：`section-header_wrapper`、`3col-grid`、`4col-grid`、`buttom-wrapper`
- 文字：`text-align-center`、`text-size-medium`、`text-size-small`、`text-color-60`、`max-width-large`、`align-center`、`gradient-headline`
- 元件：`Global Style`、`Cookies`、`Announce Bar`、`Navbar`、`Page Gradient BG`、`Section Tag` ×7、`CTA Button`、`Glass Button` ×2、`CTA Section #1`、`Footer`

**本次新建的 class 數量：0。**
保留的 7 個 `section_security-*` 只掛 `position: relative`（`-ai`、`-layers` 連這個都沒有），
原本掛在上面的手打漸層與 `color:#173044` 已移除。

---

## 4. 清理

刪除 **83 個** 孤兒 class：`section_security-bento`、`section_security-bento-v2`，
以及全部 `security-*`（`security-hero_*`、`security-bento_*`、`security-network_*`、
`security-cert_*`、`security-principle_*`、`security-ai_*`、`security-check_*`、
`security-grid_*`、`security-split_*`、`security-resource*`、`security-card_*`、
`security-section_*`、`security-tag_slot`、`security-actions_wrapper`、
`security-inline_action`、`security-reset_text`、`security-small_note`）。

刪除前已用 `query_elements` 確認頁面上沒有任何元素還掛著它們（只剩要保留的 7 個 section class）。

> ⚠️ **沒有動到** `jp-security_*`、`jp6-security-*`、`jp7-security-media`、`section_jp-security`
> —— 那些是日本頁在用的，不屬於本次範圍。

---

## 5. 文案修正

| 原文 | 處理 |
|---|---|
| 「參照日本頁的 Bento 邏輯，將身分、傳輸、雲端、備援、持續管理與國際標準整理成一眼可理解的安全架構。」 | **刪除**（內部工作註記，不該對外） |
| 「資安事件通報窗口／公開管道確認後提供」 | **刪除**（placeholder；正式通報窗口確認後再加） |
| 卡片裝飾字 `ID`、`↔`、`24/7`、`03`、`✓ …` | **刪除**（無意義符號） |
| HIPAA | 維持保守表述「依服務範圍確認適用條件」，未宣稱已取得認證 |

---

## 6. 驗證狀態

| 項目 | 結果 |
|---|---|
| 標題層級 | H1 × 1、H2 × 6、H3 × 16，無跳級 ✅ |
| 新建 class | 0 ✅ |
| 手打色碼 | 0（已全部移除）✅ |
| HTML Embed / Code Block | 0 ✅ |
| 動到共用元件／utility／Variables | 無 ✅ |
| 頁面狀態 | Draft，未 Publish ✅ |
| 繁中文案逐字校對 | 已校對；修正 2 處錯字（溲→滲、攍截→攔截、竤改→竄改）✅ |

### ⚠️ 驗證限制

- **尚未取得畫布快照。** `element_snapshot_tool` 需要 Terris 的 Designer 分頁在前景執行，
  本次呼叫回傳 `status: false`，因此**四個斷點的實際視覺（間距、換行、grid 塌陷）尚未目視確認**。
- 以下為推論而非實測，需 QA 時一併看：
  - `section-header_wrapper` 的 `padding-top: 75px` ＋ `margin-bottom: 75px` 是硬寫 px、無 RWD，
    七個 section 連續使用時手機上的累積留白可能偏大。
  - `4col-grid` 在 tiny（≤479px）會切成 `display:flex; flex-direction:column`，
    認證區四張會變成很長的一直排，需確認可接受。
  - `3col-grid` 在 medium（≤991px）**仍維持 3 欄**，只有 tiny 才變 1 欄。
    平板上三欄中文卡片是否過窄，需目視。

### 下一步

請 Terris 開啟 Designer 並保持分頁在前景，即可補做四斷點視覺 QA 與微調。
