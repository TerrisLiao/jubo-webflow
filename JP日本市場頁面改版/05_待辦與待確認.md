# 05 待辦與待確認

> 更新日期：**2026-09-08（兩頁已正式發布）**。執行順序是 P0 → P1 → P2；不要先做批次 class 清理。
>
> **兩條紅線**：①可見日文文案已由專業譯者定稿，AI 不得改寫，需要新譯文一律交回譯者；
> ②收尾工作不得改動 layout、結構與樣式（只能動 alt／aria／heading tag／component instance props 這類不影響版面的層級）。

## 已完成（2026-09-08 實測確認，不用再追）

- [x] Hero「了解 Jubo 產品」死連結 → 現在是有效外連 `mdxs.co.jp/products/jubo/?...japan_landing_v2`。
- [x] 兩頁 staging 驗收 → 已直接發布到 production。
- [x] Overview 產品區 heading 跳級 `H2 → H4 → H3` → 三張浮層卡改成 H3（只改 tag），現在全頁無跳級。
- [x] `Menifesto` → `Manifesto`（改 `Section Tag` 那一個 instance 的 prop，definition 未動）。
- [x] 圖片 alt 與 aria-label 的中文殘留 → 已改為頁面上既有的日文譯詞（清單見 `02`）。
- [x] about-us 頁尾回到頁首按鈕已上線，markup 與五條 CSS 規則都在 production 的 shared CSS 裡。

## P0：上線後仍需處理

- [ ] **Cookie 同意條仍是中文**（兩頁都是）。overview 用的是 `Cookies JP` component instance
      （`e5c6d08d-…-55f7`）、about-us 用的是全站 `Cookies`（`dc7b5f06-…-72c9`，props `hasOverride: false`）。
      日文文案需譯者提供；可以只覆寫這兩個 instance 的 `Text 1~4` props，**不要改 component definition**。
      順便決定 about-us 要不要也換成 `Cookies JP`（換 component 算結構變更，需 Terris 同意）。
- [ ] **Overview Hero CTA 文字「了解 JuboLink for Dental」是中文**，需譯者給日文。
- [ ] **修正 Overview 創辦人／核心價值黑底區的深色文字對比**；用 section 限定 combo，不改共用 `jp3-h2`／`jp3-body`。
- [ ] **about-us 表單欄位 `name` 仍是中文**（`公司名稱`／`姓名`／`電子郵件`／`電話（選填）`／`詢問內容`）。
      改它會改變表單送出的欄位名稱，可能影響通知信與外部整合 → 需 Terris 決定要不要改、以及改成什麼。
- [ ] **about-us Hero 那張 `智慧量測VitalLink+登入畫面…` 中文 alt**：要當內容圖（需譯文）還是裝飾圖（`alt=""`）？
- [ ] **⚠️ 下次 Publish 會冒出三個中文徽章「認證」**：overview 資安段第 5 張卡的 `jp-security_compliance-grid` 裡，
      四個 `jp-security_compliance-label` 徽章目前是 `認證`／`認證`／`認證`／`法規遵守`。
      前三個是中文字形，正確日文是 **`認証`**（同一張卡的項目名稱已經在用 `…マネジメントシステム認証`）。
      這四個 span **在 Designer 裡有、但目前線上的 HTML 沒有**（`jp-security_compliance-label` 在已發布頁面出現 0 次），
      所以是上次發布之後才加上去的，會在下一次 Publish 才現形。
      元素 ID（依 DOM 順序）：`e3ef925b-…-961e5`／`-961f6`／`-96205`／`-96214`（最後一個 `is-law` 已是日文）。
      這是**可見文案**，AI 未動；一個字的修正（證→証）等 Terris 或譯者確認。

## P1：結構與 spacing


- [ ] 將 JuboLink／解決方案從第 8 段整段移到 About＋數據之後，成為第 3 段。
- [ ] 拆除創辦人＋Manifesto 的雙重 section padding（2026-09-08 確認仍在：`section_manifesto` 巢狀在第 3 段 `jp3-section` 內，自帶 `padding-global padding-section-large`）。
- [ ] 逐區遷移 `jp3-section`／`jp3-container`（2026-09-08 確認 overview 仍在用 `jp3-page`／`jp3-section`／`jp3-container`／`jp3-h2`／`jp3-body`）；Desktop／Tablet／Mobile section rhythm 以 `padding-section-large` 的 8／6／4rem 為基準。
- [ ] 遷移 About Us 公司概要殘留的 `jp3-h2`／`jp3-body`：2026-08-28 以 style 廣泛查詢 `jp3-` 回 0 筆，但廣泛查詢可能延遲，需在 Designer 確認後才可結案。
- [ ] Overview Hero 圖片 alt 內容再確認（目前有 alt，但是否貼切由譯者判斷）。
- [ ] 四斷點檢查 overflow、卡片高度、文字換行與 section 留白。
- [ ] **清掉 overview body 內兩個 `<link rel="noopener" href="https://mdxs.co.jp/...">`**：`<link>` 出現在 `<body>` 是無效 HTML。
      以 MCP 用 tag `link`／屬性 `rel` 查元素都回 0 筆，查不到來源，需在 Designer 檢查那兩顆 CTA 的連結設定或自訂屬性。
- [ ] **清掉 `jp-method_image` 裡三張 `jp-replaced-media-hide` 舊圖**（換圖後留下的），只有 `jp-method_img` 是可見的。
      刪之前確認沒有 IX2／Slater 依賴。
- [ ] **合併後的「據點」列四斷點 QA**：`jp-about_profile-row` 在 767px 以下由兩欄變單欄，`jp-about_office-photo` 固定 `width: 50%`＋`aspect-ratio: 16/9` 且沒有斷點覆寫，需確認手機版照片尺寸與三個辦公室之間的 2rem 間距是否合適。
- [ ] **回到頁首按鈕四斷點 QA（2026-09-08）**：已用本機 Chromium 載入線上 `jubo-health.webflow.shared.*.css`
  ＋ Slater CSS ＋ 本次新增的五條規則做**模擬**渲染（1440／991／479 都置中、玻璃可辨、箭頭朝上、hover 末狀態
  第二顆箭頭落在正中），但那是模擬，**不是 Webflow 自己的畫面**；仍需在 Designer／staging 實看一次。
- [ ] **回到頁首按鈕 hover 實測**：hover 時箭頭應「往上滑出、第二顆從下方進來」（Slater CSS 驅動）。
  767px 以下站上 CSS 有 `.gradient-icon.is-2 { display: none }`，手機只有一顆箭頭是正常的。
  若桌機也沒有動畫，代表箭頭動畫實際上依賴 IX2 `data-w-id` 而非 Slater class selector，需改用 Glass Button component。
- [ ] **回到頁首捲動實測**：點擊應平滑捲到 hero（`#page-top`）。JP 兩頁沒有 Navbar，若 Webflow 原生 anchor
  捲動不順，再考慮其他做法（不要自行加 JS）。
- [ ] **確認不需要浮動版**：codex 那版浮動鈕已移除。若 Terris 之後要「捲動才浮現」的浮動 toggle，
  正解是在 Designer 手動加 IX2，不是回頭用 page custom code 的 JS（見 `03` §6）。
- [ ] **確認 page custom code 清空沒有連帶影響**：移除前該 footer 區塊只有 `.jp-back-to-top` 的 style／button／script，
  沒有其他程式碼；已讀回確認 head／footer 皆為空字串。發布前再確認一次頁面沒有依賴那段程式碼的地方。
## P2：Class 清理候選

以下只代表「目前兩頁未使用或疑似舊版本殘留」，刪除前仍要查全站使用、IX2 與 Slater：

- `jp-team_tag-text`
- `jp-method_flow-arrow`
- `is-flow-top`／`is-flow-right`／`is-flow-bottom`／`is-flow-left`
- `jp-overview_nav-btn`／`is-hero-top-right`
- `jp-overview_nav-btn-text`／`jp-overview_nav-btn-icon`

`Image 2` 仍套在三張歷程 logo 上；先比較四斷點全部屬性，若為空才從元素卸除並清理。

## 明確不可刪

- `cascading-slider*` 與 Slater selector 使用的 class。
- Slider nav、arrow、`demo_hide`，除非拆除整個 slider 後完成實測。
- Hero overlay、glow、gradient 等空白視覺層。
- 廠商共用 component、utility、variables。
- 仍在頁面使用的 `jp3-*`／`jp-partner-*`；它們需遷移，不是直接刪除。

## 內容／素材待確認

- [ ] COO 陳依伶照片與是否加入領導團隊。
- [ ] 日本市場據點資料與 CMS draft 狀態（CMS「全球服務據點」第 4 筆日本業務仍是 draft 且無地址，頁面上未呈現）。
- [ ] 合併後新增的辦公室子標籤（台北研發總部／台南辦公室／北美分公司）與 H2「公司概要」需納入日文校稿範圍。
- [ ] 台北電話 `（02）5568-6435` 同時出現在「據點」與「聯絡方式」兩列（Terris 已決議兩處都留）；若日文版想精簡再回頭處理。
- [ ] 公司概要「事業內容」與領導團隊短 bio 最終核可。
- [ ] 歷程年份與日本／北美節點的事實核對。
- [ ] 日文老師校稿與 SEO title／description。

## 已完成／已取代

- [x] 舊部門 cascading slider 已從 Overview 刪除。
- [x] About Us Hero 已保留，舊「不做 Hero」決議作廢。
- [x] Jubo 歷程三張圖片已調亮、卡片底部 logo 已置中。
- [x] Overview Hero 標題已縮小至接近首頁尺度。
- [x] About Us 主要 section 已統一使用廠商標準 spacing 結構。
- [x] 服務據點段落已併入公司概要表「據點」列，舊 section 已刪除（2026-08-28）。
- [x] 公司概要段落補上 H2「公司概要」。
- [x] 頁面 Draft 狀態疑慮結案：兩頁 `draft: false` 是 Terris 為了發布到 staging 檢視刻意設定，非異常（2026-08-28 Terris 確認）。

## 發布前檢查

- [ ] 每頁恰有一個 H1，heading 層級連續。
- [ ] 所有連結與表單完成實際點擊／送出測試。
- [ ] 所有圖片 alt 完成。
- [ ] 四斷點視覺驗收完成。
- [ ] 未修改共用 component、utility、variables。
- [ ] 日文與 SEO 完成。
- [ ] 取得明確 Publish 授權。
