# 02 頁面現況規格（as-built）

> 讀回日期：2026-08-20。這是 Webflow 即時結構快照，不代表視覺已通過 QA；後續 Designer 改動必須同步更新本檔。

## `/jp/about-us`

Page ID：`6a8537247cf50018fca7c68d`

> 段落結構讀回日期：2026-08-28（服務據點併入公司概要之後）。

| # | 段落 | Section Element ID | 結構 |
|---|---|---|---|
| 1 | Hero | `e02e11ae-…-c9a9f` | `section_about-hero` → `padding-global padding-section-large` |
| 2 | 公司概要（含服務據點） | `0358a1b5-…-e51d` | `section_about is-has-logo` → 標準 padding／container |
| 3 | 經營團隊 | `8bc186e6-…-4855` | `section_about` → 標準 padding／container |
| 4 | 為什麼是日本／MDX | `a513…36fb` | `section_about` → 標準 padding／container |
| 5 | 日本市場詢問表單 | `3b015b9a-…-aa8b` | `section_about` → 標準 padding／container |
| 6 | 回到頁首（Back to top） | `f1eaa147-…-3a99` | `section_about` → `padding-global padding-section-small` → `container-large` → `jp-about_to-top-wrap` |

結構狀態：

- 前五個 section 全部使用 `padding-global padding-section-large`；第 6 段（回到頁首）刻意用 `padding-section-small`。非 Hero section 使用 `container-large`。
- `main-wrapper` 本身是語意 `<main>`。
- 一個 H1（Hero「公司資訊」），其餘主要標題為 H2；DOM heading 順序為 H1 → H2 公司概要 → H2 領導團隊 → H2 結合台灣實務經驗 → H3 ×3 → H2 與 Jubo 一起拓展日本市場，層級連續。第 6 段（回到頁首）之外無 HtmlEmbed。
- Hero 使用既有 `product-hero_*` 結構與共用工具類。
- Hero 四張照片：三張沒有 alt（視為裝飾），一張仍是中文 alt `智慧量測VitalLink+登入畫面…`。
  這張要當內容圖（需要日文譯文）還是裝飾圖（`alt=""`）由 Terris 決定，**未動**。
- 2026-09-08 已把其餘中文 alt 換成頁面上既有的日文譯詞：三張據點照 → `台北本社・研究開発本部`／`台南オフィス`／`北米法人`；
  四張領導團隊照 → `康仕仲｜創業者・CEO`／`謝美芬｜副CEO・CRO（最高収益責任者）`／`古凱元｜CTO（最高技術責任者）`／`李泓其｜CDO（最高デジタル責任者）`。
- 表單五個欄位的 `aria-label` 已由中文改為頁面上的日文標籤：`会社名`／`お名前`／`メールアドレス`／`電話番号（任意）`／`お問い合わせ内容`。
  **但欄位的 `name`（`公司名稱`／`姓名`／`電子郵件`／`電話（選填）`／`詢問內容`）仍是中文，未動**——改它會改變表單送出的欄位名稱，可能影響既有通知信與外部整合，需 Terris 決定。
- `FormTextInput`／`FormTextarea` 用 `data_element_tool > set_attributes` 寫 attribute 會回 internal error（同 `06` §4）；
  改用 `data_element_settings_tool > set_settings` 的 `attributes` key 才寫得進去（該 key 會整份取代 attribute 清單，`id` 是獨立的 `domId` setting，不受影響）。
- 以 style 廣泛查詢 `jp3-` 回 0 筆（2026-08-28）。因廣泛查詢可能延遲，結案前仍須在 Designer Style Manager 確認。
- 2026-09-08 新增第 6 段「回到頁首」：唯一內容是一顆 72px 圓形 glass 按鈕
  （`glass-button` → `glass-element is-to-top` → `jp-about_to-top-icons` → 兩顆 `gradient-icon`），
  段落沿用 `section_about` 與 `padding-global padding-section-small`（其餘五段是 `padding-section-large`，這段刻意較窄）。
- 同日移除 codex 版的浮動回到頁首鈕（原本寫在 Page Settings → Custom Code → Footer 的 `.jp-back-to-top`）。
  **該頁 head／footer 自訂程式碼現在都是空的**；原始碼存放於 `custom-code/archive/`。
- 該段內含**兩個 HtmlEmbed**（`gradient-icon is-1` / `is-2` 的箭頭 SVG）。在此之前 about-us 的 HtmlEmbed 數是 0，
  現在是 2；這是站上 `.gradient-icon` 既有的實作方式（Glass Button component 內部也是這樣），不是用 Embed 畫版面。
- Hero section `534fcc9e-…-f26f` 於 2026-09-08 加上 DOM id `page-top`，作為回到頁首的 anchor 目標。
- 元素／class 數量仍是 2026-08-20 快照（258 元素、54 distinct class、185 次套用），本輪合併後未重新統計。

### 公司概要段落內部結構（2026-08-28 合併後）

- Section header `dae3e142-…-25e9`：eyebrow`公司基本資訊`＋ H2`公司概要`（`6a595c34-…-a69b1`，class `jp-about_h2`）。
- 表格 `jp-about_profile-table`（`bfec233a-…-d67e`）共 11 列 `jp-about_profile-row`。
- 「據點」列 `ca779fe7-…-53fe`：label`據點`＋值欄位 `jp-about_profile-lines is-offices`（`395224d9-…-ed2b`），內含三個辦公室區塊（台北研發總部 `afca809c`／台南辦公室 `85fc9c2d`／北美分公司 `0a67fd0c`）。
- 每個辦公室區塊順序：辦公室名 `jp-about_profile-label` → 照片 `jp-about_office-photo` → 地址 `jp-about_profile-value` → 電話／Email `jp-about_profile-value` → `在 Google 地圖查看` `jp-about_office-link`（`linkType: url`，Google Maps 短網址）。
- 三個辦公室區塊的外層 div 目前**沒有 class**（合併前就沒有，未新增）；靠 `is-offices` 的 `row-gap: 2rem` 分隔。
- 舊「服務據點」section `cdd74f9d-…-e4ae`（含 eyebrow`公司辦公室`與第二張 `jp-about_profile-table`）已於 2026-08-28 刪除。

## `/jp/overview`

Page ID：`6a848bfa57437623d952f944`

> 結構讀回日期：**2026-09-08**（`get_all_elements`，507 個節點）。取代 2026-08-20 那份 8 段表——那份已經對不上站上實況。

| # | 區塊 | Section Element ID | 主要 class／備註 |
|---|---|---|---|
| 1 | Hero | `1466e10a-…-51d7c` | `jp-partner_hero-full`；左上 `jp-hero-jubo-logo is-top-left`（`role="img"` + `aria-label="Jubo"`）、`jp-overview_hero-nav` 內是 **Glass Button** instance、`jp-partner-hero-image-layer`、`jp-partner_hero-glow`、H1 `jp-partner_hero-title` |
| 2 | 關於 Jubo＋數據 | `973d9fb3-…-2e60c` | `section_home-about`（`id="about-jubo"`）＋ `padding-global padding-section-large` ＋ `container-large`；H2 ＋ 三個 `stats-h3` |
| 3 | 創辦人 | `708363d9-…-21cb8` | `jp3-section`／`jp3-container`／`jp-ceo_grid`；H2 `jp3-h2` ＋ H3 `jp-ceo_name` |
| 3-1 | Manifesto（**巢狀在第 3 段內**） | `30826d2e-…-c35a4` | `section_manifesto`（`id="manifesto"`）自帶 `padding-global padding-section-large` ＋ `container-small` → **雙重 section padding 仍存在**；eyebrow 是 `Section Tag` component instance |
| 4 | JuboLink for Dental（解決方案） | `ef66fb18-…-51c8` | `jp3-section`／`jp3-container`；`jp-solutions_header`（H2 `jp3-h2` 含 `gradient-headline`）＋ `jp-slider_swiper-wrap` → `jp-slider`（Swiper，`data-autoplay="false"`）→ slide 內 `jp-partner-v4 is-slider`（`id="solutions"`） |
| 5 | Jubo 歷程 | `ec608427-…-2b14` | `section_jp-journey`（`id="journey"`）／`jp-journey_container`；H2 ＋ 三張 `jp-journey_title` H3；含三個 `jp-journey_mdx-logo` |
| 6 | 理念 | `ef66fb18-…-50cf` | `jp3-section`（`id="about"`）／`jp3-container jp-about_layout`；H2 `jp3-h2` ＋ 三個 `jp-about_title` H3 |
| 7 | 團隊與方法論 | `ef66fb18-…-5163` | `jp3-section`／`jp3-container`；`jp-method_intro`（H2）＋ 四個 `jp-method_title` H3 ＋ `jp-method_image` |
| 8 | 資訊安全 | `b6fb27b5-…-e652` | `section_jp-security`／`jp3-container`；H2 ＋ 五張 `jp-security_bento`（各一個 `jp-security_bento-title` H3），第 2 張內含 `jp-security_layer-grid` 四層卡、第 5 張內含 `jp-security_compliance-grid` |
| 9 | 結尾 CTA | `22cdf9be-…-cc240` | **`CTA Section #3` component instance**（不是頁面元素） |

外層：`page-wrapper page-jp` → `Global Style` ＋ **`Cookies JP`** instance（`e5c6d08d-…-55f7`）→ `main-wrapper`（div）→ `Page Gradient BG` instance → `all-sections-wrapper` → `jp3-page`（語意 `<main>`）。

結構狀態（2026-09-08）：

- **Heading 順序已修正**：DOM 順序為 H1 → H2 → H3×3 → H2 → H3 → H2(Manifesto) → H2 → **H3×3 → H3 → H4** → H2 → H3×3 → H2 → H3×3 → H2 → H3×4 → H2 → H3×5，沒有跳級。
  第 4 段原本是 H2 → H4×3 → H3 → H4（`jp-partner-v4_merit-title` 三張浮層卡在 `jp-partner-v4_media` 裡、產品標題 H3 在後面的 `jp-partner-v4_panel`），
  今日把那三張浮層卡改成 H3（只改 tag、沒動 DOM 順序與樣式），`診療報酬` 那張維持 H4 掛在產品 H3 底下。
- `jp3-*` 仍在使用：`jp3-page`／`jp3-section`（第 3、4、6、7 段）／`jp3-container`／`jp3-h2`／`jp3-body`。P1 的遷移**尚未執行**。
- `jp-method_image` 內有 **4 張圖，其中 3 張掛 `jp-replaced-media-hide`**（換圖後留下的舊圖），只有 `jp-method_img` 是可見的。屬清理候選。
- Hero 主 CTA 已是有效外連（`mdxs.co.jp/products/jubo/?...utm_campaign=japan_landing_v2`，`target="_blank"`），**不再是 `link: null`**。
- 但已發布的 HTML 裡有 **兩個 `<link rel="noopener" href="https://mdxs.co.jp/...">` 出現在 `<body>` 內**（`<link>` 放 body 是無效 HTML）。
  以 MCP 用 tag `link` 與屬性 `rel` 查元素都回 0 筆，查不到來源，需在 Designer 檢查那兩顆 CTA 的連結／自訂屬性設定。
- 圖片 alt：2026-09-08 已把殘留中文 alt 換成頁面上既有的日文譯詞（MDX logo ×3 → `MDX`；五張 `jp-security_media` → 各自卡片的 H3；`jp-method_image` 內隱藏舊圖 → 沿用可見圖的 alt）。
  `jp-security_compliance-grid` 的 `aria-label` 由 `Jubo 國際認證與法規遵循` 改為 `医療品質・情報セキュリティ基準`。
- Manifesto eyebrow 的錯字 `Menifesto` 已改為 `Manifesto`——改的是**那一個 instance 的 prop**，`Section Tag` component definition 未動。
- Cookie 同意條用的是 **`Cookies JP`** component，但**內容文字目前仍是中文**（拒絕全部／接受全部／Cookie 偏好設定…）。需要譯者提供日文，AI 不自行翻譯。

## 目前頁面順序決策

JuboLink 目前仍在第 8 段。最新建議是移到「關於 Jubo＋數據」之後成為第 3 段；此項尚未執行，不可在 as-built 文件中寫成已完成。

## 驗證限制

2026-08-28 合併作業：結構與 class 掛載已用 `element_id` 精確查詢讀回驗證；`element_snapshot_tool` 回 `status: false`（無可用 Designer session），因此**四斷點視覺與 computed style 未經驗證**，仍待人工 QA。

2026-09-08 回到頁首按鈕：結構、class 掛載、`link`、`aria-label`、兩個 Embed 的 SVG 內容都已用 `element_id` 精確查詢讀回驗證；新增的兩個 style 已用 `query_styles` 四斷點讀回。`element_snapshot_tool` 同樣回 `status: false`（Designer session 未連線），**四斷點視覺、hover 動畫與捲動行為未經驗證**，需 Terris 在 Designer／staging 實測。

Webflow 連接器可讀元素樹與 class 套用，但本輪無法可靠取得所有 class 的完整跨站使用次數與每個斷點全部 computed properties。任何 class 刪除仍須在 Designer Style Manager 與 Slater selector 清單做最後確認。
