# 07 變更歷程

> 每一筆變更都有對應的 git commit（分支 `agent/jp-company-design-handoff`），
> 細節看 `git log`；這份只保留**決策的來龍去脈**，方便追溯「為什麼會變成現在這樣」。
>
> 全部發生在 **2026-08-19**（單日多輪迭代）。

## 第一階段：對正事實與結構定案

| 變更 | 摘要 |
|---|---|
| 現況查證 | 透過 MCP 逐項查證，發現實況比原交接文件描述的更空（component 0 個、Image 0 個、tag 全是 div） |
| 頁面更名 | `/jp/company` → `/jp/about-us`（頁面從未發布，無 SEO 風險） |
| P0 結構修復 | 補上 Global Style／Cookies／Page Gradient BG；`main-wrapper` tag 改 `main`；section tag 改 `section` |
| 結構轉向 | 主要表達形式由**卡片改為條列／表格**（Terris：「用卡片做很難讀」） |
| 兩頁分工 | about-us 專注公司資料；創辦人／Manifesto／歷程／團隊留在或移到 overview |
| 受眾定位 | 確立 about-us 面向**代理商與合作夥伴**，購買意圖導向 MDX |
| 頁面瘦身 | 移除 Hero、Jubo 歷程、資本與策略夥伴、多職能團隊、創辦人段 |
| overview 新增 | 加入「團隊組成」cascading slider 四格（複製自 `/internship`）；Manifesto 移入創辦人段下方 |

## 第二階段：內容與資料

| 變更 | 摘要 |
|---|---|
| 公司概要表定案 | 11 列 label/value；新增資本額 9.9 億元、從業人員 126 人；事業內容改四行條列 |
| 移除欄位 | 「認證與獲獎」「投資人與策略夥伴」兩列移除（Terris 決議，CMS 資料保留） |
| 北美分部 | overview 團隊組成第四張卡「國際拓展」改名「北美分部」，文案與職能標籤一併改寫 |
| 經營團隊 | 新增四張 CxO 卡（CEO／CRO／CTO／CDO）；COO 待照片更新後再加 |
| 服務據點 | 三個據點加上照片（來源為 CMS Cover Image，另存為獨立 asset） |

## 第三階段：經營團隊卡片的多輪視覺迭代

這一段來回最多，最終收斂結果與理由見 `03_設計決策紀錄.md` 第 4 節。過程摘要：

2×2 玻璃疊層卡 → 縮小照片／拉長版面 → **修正誤解**（玻璃卡應疊在照片內，不是把照片切小露底色）
→ 修掉照片底部露灰的 bug（`<img>` 缺 `height:100%`）＋卡片比例對齊照片原始比例 2:3
→ 試做白色留白外框（**否決**）→ 試做玻璃質感外框（**否決**）→ 回到滿版
→ 玻璃面板改固定高度使四卡頂齊 → 間距修正（縮小責任從卡片移到網格）
→ 加入 2×2 階梯錯落排版 → 字級與顏色微調

> 中間一度把「間距太遠」誤判成 gap 值問題，實際根因是卡片自己置中留白疊加 gap。

## 第四階段：表單、導覽與 logo

| 變更 | 摘要 |
|---|---|
| 表單標籤 | 標籤誤用窄欄 class 導致逐字換行 → 一度改成無標籤（**欄位全空**，因為 MCP 設不了 placeholder）→ 改用 `jp-about_field-label` 顯性標籤 |
| 表單版面 | 加 `jp-about_form-wrap` 置中；詢問內容欄位加 `jp-about_field-full` 讓底線撐滿整列 |
| 章節順序 | 調整為 公司概要 → 經營團隊 → 服務據點 → 表單 |
| 導覽（錯誤方向） | 自建固定 header bar 放「企業情報」按鈕 —— **與站上既有語彙衝突，已撤銷** |
| 導覽（正解） | 按鈕改為 hero 右上角絕對定位，與既有左上角 logo 對稱 |
| 按鈕動畫 | 手刻按鈕**沒有動畫** → 改用既有 **Glass Button component**（Slater 文字動畫 + IX2 箭頭動畫） |
| about-us logo | PNG 含大量透明留白導致尺寸與定位不可控 → 改用與 overview 相同的 inline SVG，品牌藍 |

## 2026-08-28：服務據點併入公司概要

| 變更 | 摘要 |
|---|---|
| 合併來由 | 日文口譯指出「公司概要表」與「服務據點」重覆同一份資料，建議合併；Terris 決議把全部資訊收進概要表的「據點」列 |
| 據點列改寫 | 三個辦公室區塊（名稱／照片／地址／電話 Email／Google 地圖連結）搬入「據點」列值欄位；原三行純文字段落刪除 |
| 新增 class | 只新增 combo `jp-about_profile-lines.is-offices`（`row-gap: 2rem`）；未修改任何既有 class 的值，未新增 HTML Embed |
| 標題 | 原「服務據點」H2 搬到公司概要段落並改字為「公司概要」——合併前該段沒有 H2 |
| Email | 台北研發總部 `info@jubo.health` → `terrisliao@jubo.health`（Terris：兩處都留 terrisliao） |
| 刪除 | 舊「服務據點」section `cdd74f9d-…-e4ae` 整段刪除，頁面 6 段 → 5 段 |
| 驗證 | 結構以 `element_id` 精確查詢讀回確認；`element_snapshot_tool` 回 `status: false`，四斷點視覺**未驗證** |
| Draft 狀態 | 讀回發現兩頁皆 `draft: false`，一度視為異常回報；Terris 確認是本人為了在 staging 檢視刻意發布，非異常。相關文件（`README`、`01`、`03`、`05`、`08`）的「保持 Draft」記載已一併校正為「AI 不得自行 Publish，發布權在 Terris」 |

## 文件重整（本次）

原本單一檔案 `JP_COMPANY_REDESIGN_HANDOFF.md` 累積到 1070 行，且結構已經不成立：
編號順序是 §1–5 → §5-1…5-10 → §6–11 → §5-11…5-27（後段是逐輪追加的紀錄，
被接在原文件最後而不是插在對應位置），而原文件的 §6 線框圖與 §10 待辦清單
描述的是**已經被移除的版面**（Hero、歷程、資本與策略夥伴），留著會誤導接手者。

因此拆成本資料夾的七份文件：現況（02）／決策理由（03）／識別碼（04）／
待辦（05）／實作陷阱（06）／歷程（07），各自單一用途。


## 2026-08-20 — 即時結構與文件整合

- Terris 刪除 Overview 舊部門 cascading slider；保留「不同專業一起」四角方法論區。
- Overview Hero 標題縮小；Journey 三張圖調亮、logo 置中。
- About Us 即時結構確認為 6 段，包含 Hero 與「為什麼是日本／MDX」。
- 完成兩頁 spacing／class／可讀性嚴格稽核；新增 `11_2026-08-20_樣式與結構稽核.md`。
- 把 PR 文件從早期 handoff 更新成現況、P0/P1/P2 與歷史狀態，移除互相矛盾的「已完成」說法。
- 本次只更新 repository 文件，未 Publish、未移動 JuboLink、未刪 Webflow class。

## 2026-09-08 — About Us 新增回到頁首 glass 按鈕

| 變更 | 摘要 |
|---|---|
| 新增段落 | `/jp/about-us` 末端新增第 6 段 `section_about`（`f1eaa147-…-3a99`），只放一顆回到頁首按鈕；用 `padding-section-small` 而非 `padding-section-large` |
| 按鈕結構 | `glass-button` → `glass-element is-to-top` → `glass-effect is-icon` ＋ 7 層 `glass-effect__* is-icon` → `gradient-icon is-1` / `gradient-icon is-2 is-to-top` |
| 新增 class | 三個：`jp-about_to-top-wrap`、combo `glass-element.is-to-top`、combo `gradient-icon.is-2.is-to-top`。**未修改任何既有 class 的值** |
| 箭頭朝上 | 靠 `is-to-top` 的 `transform: rotate(-90deg)` 旋轉外層膠囊（沿用站上 `is-icon` 的 `rotate(-45deg)` 手法）；箭頭自己的 transform 被 Slater `!important` 鎖住不能動 |
| 新增 Embed | 兩個 HtmlEmbed（箭頭 SVG）。about-us 的 HtmlEmbed 由 0 → 2；SVG 內容與站上 CTA 同一顆，只換掉 `mask`／`linearGradient` 的 id 避免全頁 id 重複 |
| 捲動 | Hero section `534fcc9e-…-f26f` 加 DOM id `page-top`；按鈕 `linkType: url`、`link: #page-top`（避開 `06` §1 的 `linkType: page` 靜默失敗）。未寫任何 JS |
| 無障礙 | `<a>` 掛 `aria-label="ページトップへ戻る"`；兩顆 SVG `aria-hidden="true"` |
| Designer 命名 | Section = `Section Back To Top`，連結 = `Back To Top Glass Button` |
| 驗證 | 結構、class、`link`、`aria-label`、兩個 Embed 的 code、三個 style 全部以 `element_id`／`query_styles` 讀回確認 |
| 驗證限制 | `element_snapshot_tool` 回 `status: false`（無 Designer session）→ **四斷點視覺、hover 動畫、捲動行為未驗證**，QA 項目已寫進 `05` |
| Publish | **未 Publish**，僅寫入 Designer |

## 2026-09-08（第二輪）— 移除 codex 浮動鈕、玻璃改為可辨識並放大

| 變更 | 摘要 |
|---|---|
| 移除浮動鈕 | codex 版 `.jp-back-to-top` 寫在 Page Settings → Custom Code → Before `</body>` tag（自訂 CSS ＋ `<button>` ＋ JS，右下角深青實心圓、捲動 0.75 視窗高後淡入）。Terris 在 Designer 手動刪除，API 端同時 `set_page_freeform_code` 清空；讀回確認該頁 head／footer 自訂程式碼皆為空字串 |
| 存檔 | 原始碼保存到 `custom-code/archive/jp-about-us_page-footer_back-to-top_2026-09-08.html`，可還原 |
| 玻璃太不明顯的原因 | 第一版疊了 `glass-effect is-icon` ＋ 7 層 `glass-effect__* is-icon`，其中 `.glass-effect__fill.is-icon` 是 `#f7f7f7cc`（近乎不透明淺灰），結果是灰色圓盤、`backdrop-filter` 看不出來。那套材質層是為 CTA 裡 28px 小徽章調的，放大不成立 |
| 改法 | 拆掉 8 個 `glass-effect*` 元素，玻璃改用本頁 `jp-about_leader-glass` 的配方（135° 白→淡藍漸層＋`blur(40px) saturate(190%)`＋1px 白邊），再加外陰影與內側高光 |
| 放大 | 圓形 `3.25rem`（52px）→ `4.5rem`（72px）；箭頭 `1.125rem` → `1.75rem`（28px） |
| 旋轉層搬家 | 新增 `jp-about_to-top-icons` 只包兩顆箭頭並承接 `rotate(-90deg)`；原本旋轉掛在 `glass-element is-to-top` 上會讓玻璃光源從左下來，現在玻璃維持正常方位 |
| 新增／更新 class | 新增 `jp-about_to-top-icons`、`.gradient-icon.is-1.is-to-top`；更新 `.glass-element.is-to-top`（尺寸／玻璃配方，移除 `background-color` 與 `transform`）、`.gradient-icon.is-2.is-to-top`（`left: calc(50% - 0.875rem)`、放大）。仍未修改任何既有共用 class 的值 |
| 驗證 | 結構與五個 style 全部 `element_id`／`query_styles` 讀回確認；另用本機 Chromium 載入線上 `jubo-health.webflow.shared.*.css` ＋ Slater CSS ＋ 本次五條規則做**模擬**渲染（1440／991／479），確認置中、玻璃可辨、箭頭朝上、hover 末狀態第二顆箭頭落在正中 |
| 驗證限制 | 模擬渲染**不是** Webflow 自己的畫面；`element_snapshot_tool` 仍回 `status: false`（Designer session 未連線）。實機 QA 項目留在 `05` |
| Publish | **未 Publish** |

## 2026-09-08（第三輪）— 上線後收尾：中文殘留、heading 跳級、錯字、文件校正

前提：Terris 已把兩頁正式發布到 production，日文文案由專業譯者定稿。本輪紅線是**不動 layout／結構／樣式，也不改寫可見日文文案**。

| 變更 | 摘要 |
|---|---|
| about-us 圖片 alt | 7 張中文 alt → 頁面上既有日文譯詞：三張據點照 `台北本社・研究開発本部`／`台南オフィス`／`北米法人`；四張領導照 `康仕仲｜創業者・CEO`／`謝美芬｜副CEO・CRO（最高収益責任者）`／`古凱元｜CTO（最高技術責任者）`／`李泓其｜CDO（最高デジタル責任者）` |
| about-us 表單 aria-label | 5 個中文 → `会社名`／`お名前`／`メールアドレス`／`電話番号（任意）`／`お問い合わせ内容`（全部取自頁面上的 `jp-about_field-label`） |
| 寫入方式 | `FormTextInput`／`FormTextarea` 用 `set_attributes` 一律回 internal error（同 `06` §4）；改用 `set_settings` 的 `attributes` key 成功。該 key 整份取代 attribute 清單，但 `id` 是獨立的 `domId` setting，不受影響（已讀回確認 `jp-company` 等仍在） |
| overview 圖片 alt | 9 張：MDX logo ×3 → `MDX`；五張 `jp-security_media` → 各自卡片的 H3（`IoT機器連携`／`情報セキュリティとプライバシー保護`／`データバックアップとシステム復旧`／`情報セキュリティの継続的な強化`／`医療品質・情報セキュリティ基準`）；`jp-method_image` 內隱藏舊圖 → 沿用可見圖的 alt |
| overview aria-label | `jp-security_compliance-grid`：`Jubo 國際認證與法規遵循` → `医療品質・情報セキュリティ基準` |
| heading 跳級 | 第 4 段原為 H2 → H4×3 → H3 → H4。把 `jp-partner-v4_media` 內三張浮層卡的 `jp-partner-v4_merit-title` 由 h4 改 h3（**只改 tag，未動 DOM 順序、未動樣式**），`診療報酬` 那張維持 h4 掛在產品 H3 底下。全頁不再跳級 |
| 為何只改 tag 不重排 | 三張浮層卡在 media 容器、產品標題在後面的 panel 容器；要讓 h4 排在 h3 之後就得動 DOM／版面，違反本輪紅線。改 tag 沒有視覺影響：`.jp-partner-v4_merit-title.is-dental-glass-title`（0,2,0）已明確設定 margin／font-size／font-weight／line-height／color，特異度高於 `h3`／`h4` tag selector（0,0,1） |
| 錯字 | Manifesto 區 eyebrow `Menifesto` → `Manifesto`。改的是那一個 `Section Tag` **instance 的 prop**，component definition 未動 |
| 沒有動的（需譯者或 Terris 決定） | Cookie 同意條全中文（overview 用 `Cookies JP`、about-us 用 `Cookies`）／overview Hero CTA「了解 JuboLink for Dental」／about-us 表單欄位 `name` 仍中文（改動會影響表單送出欄位名與外部整合）／about-us Hero 那張 `智慧量測VitalLink+…` alt 要當內容圖還是裝飾圖 |
| 新發現 | overview 已發布 HTML 的 `<body>` 內有兩個 `<link rel="noopener" href="https://mdxs.co.jp/...">`（無效 HTML）；MCP 以 tag `link`／屬性 `rel` 查元素都回 0 筆，來源要在 Designer 查。另外 `jp-method_image` 有三張 `jp-replaced-media-hide` 舊圖待清 |
| 文件 | `02` overview 段落表整份改寫成 2026-09-08 讀回的 9 區塊現況（舊 8 段表已對不上）；`05` 加上「已完成」區塊並重排 P0；`README`／`01`／`08_中文文案交接` 的「繁中草稿／staging」字眼改成已發布；`00` 加上日文文案紅線與 JP 頁 Draft 例外；全站 `08_頁面與資訊架構` 由 53 頁補到 57 頁並修正 `/ai/amy` 路徑 |
| 驗證 | about-us：alt、aria-label、`domId` 全部 `element_id` 精確讀回確認。overview：三顆 heading tag 已讀回為 `h3`、`診療報酬` 仍 `h4`、`Manifesto` prop 已讀回；**alt／aria 的讀回被 Webflow API 429 擋住，尚未逐項讀回確認** |
| Publish | **未 Publish**（本輪改動仍停在 Designer，需 Terris 再發布一次才會上線） |
